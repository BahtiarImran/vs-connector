<UserControl x:Class="ThoughtWorks.VisualStudio.MurmurViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
             
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="66"></RowDefinition>
        </Grid.RowDefinitions>
        <ListBox x:Name="murmursList" Grid.Row="0" BorderThickness="0" Margin="8,0,8,8" IsSynchronizedWithCurrentItem="True" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{Binding Path=Body}" TextWrapping="Wrap" Height="Auto"/>
                        <StackPanel Orientation="Horizontal" >
                            <TextBlock Text="{Binding Path=Name}" Foreground="DimGray" />
                            <TextBlock Text="on"  Margin="6,0,6,0"   Foreground="DimGray"  />
                            <TextBlock Text="{Binding Path=Date}"  Foreground="DimGray"  />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Height="66" Grid.Row="1" VerticalAlignment="Bottom" Margin="8,0,0,8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="80"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="murmurText" Grid.Column="0" HorizontalAlignment="Stretch"
                                BorderThickness="2" BorderBrush="LightGray"
                                Height="48"
                                AcceptsReturn="False" AutoWordSelection="True" TextWrapping="WrapWithOverflow"
 			                    />
        <Button x:Name="buttonMurmur" VerticalAlignment="Top" Grid.Column="1"
						    HorizontalAlignment="Center" Margin="0,8,0,0"
						    Content="Murmur This" 
						    FontSize="11" 
						    Height="20" Click="OnClickButtonMurmur" Background="Gainsboro" BorderThickness="0" Opacity="1" IsDefault="True" />
        </Grid>
    </Grid>
</UserControl>
